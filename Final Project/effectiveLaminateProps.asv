function [out] = effectiveLaminateProps(Q, t, ss)

% Laminate total thickness
h = t * length(ss); % in

effectiveLaminatePropsMech.Ex = 1 / (h * Q(1, 1));
effectiveLaminatePropsMech.Ey = 1 / (h * Q(2, 2));
effectiveLaminatePropsMech.Gxy = 1




end
